# 기본 차단 정책 (Deny All)
# 명시적 NetworkPolicy에 해당하지 않는 모든 트래픽 차단
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: hwp-converter
  labels:
    app.kubernetes.io/part-of: hwp-converter
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
